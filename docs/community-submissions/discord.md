# Discord post

```text
I made a DSH plugin for a problem I kept hitting in long agent sessions: a side question about an old answer would contaminate the main task, while a normal sidebar thread was still just one more linear conversation.

dsh-nested-followups lets you branch from any completed answer, then branch again from answers inside that side trail. Every level is a real isolated session. The main chat stays untouched, branch tools are read-only, and Tree View keeps the recursive trail attached to the root conversation.

13-second real DSH demo + install: https://github.com/sluminositys/dsh-nested-followups
```

If the server has a dedicated plugin channel, post there and attach
`assets/demo.gif`; otherwise send only the repository link so Discord generates
the social-preview card.
